#include<stdio.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char **argv){

 char *run= "python3 translate.py ";
// char *run= malloc((strlen(argv[1])+strlen(argv[2])+1)*sizeof(char));
// strcpy(run, python);
// strcat(run, argv[1]);


 for(int i=1; i < argc; i++){
  char *tmp= char/
 malloc((strlen(run)+1)*)
 }

 system (run); 
 return;

}
